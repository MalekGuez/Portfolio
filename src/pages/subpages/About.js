import React from 'react';

const About = ({}) => {
    return (
        <div className="about__container">
            <div className="title---circles">About me</div>
        </div>
    );
};

export default About;